# Allow installs to proceed even if peer dependency versions conflict (e.g., React 19 vs packages expecting React 18)
legacy-peer-deps=true
# Also relax strict peer dependency enforcement (some CI providers enable this by default)
strict-peer-deps=false
